[project]
name = "pemrix"
version = "0.1.0"
description = "Python SDK for the PEMRIX network"
readme = "README.md"
license = { text = "MIT OR Apache-2.0" }
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
