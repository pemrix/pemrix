---
title: Nitro Variant
description: High-speed model inference with :nitro
seoTitle: 'Nitro Variant | High-Speed Inference'
og:site_name: OpenRouter Documentation
og:title: 'Nitro Variant - High-Speed Inference'
og:description: "Access high-speed model inference using the :nitro variant."
og:image: https://openrouter.ai/dynamic-og?title=Nitro%20Variant&description=High-speed%20inference
og:image:width: 1200
og:image:height: 630
twitter:card: summary_large_image
twitter:site: '@OpenRouter'
noindex: false
nofollow: false
icon: bolt
sidebarTitle: Nitro
---

The `:nitro` variant optimizes for speed. When you use `:nitro`, OpenRouter sorts the model's providers by throughput (tokens per second) and also makes [priority service tier](/guides/features/service-tiers) endpoints eligible, so a provider's priority tier can serve the request when it is genuinely the fastest option.

## Usage

Append `:nitro` to any model ID:

```json lines
{
  "model": "openai/gpt-5.2:nitro"
}
```

## How it works

`:nitro` does two things:

1. **Sorts all eligible endpoints by throughput**, the same effect as setting [`provider.sort` to `"throughput"`](/guides/routing/provider-selection#provider-sorting).
2. **Admits priority service tier endpoints** into the eligible pool. Unlike the [`service_tier: "priority"` parameter](/guides/features/service-tiers), priority endpoints get no special treatment: they compete with every other endpoint on measured throughput, and win only when they are actually the fastest.

Because priority tier endpoints are billed at priority rates, a `:nitro` request served by a priority endpoint is charged that endpoint's priority pricing. As always, billing follows the tier the provider actually serves: if the provider sheds the request to its default tier, you're billed the default rate. See [Service Tiers](/guides/features/service-tiers) for the full comparison of tier-selection options.

If you want throughput sorting without priority tier eligibility, set `provider.sort` to `"throughput"` instead of using `:nitro`.
